# ═══════════════════════════════════════════════════════════════
#  All apps & CLI tools live here. Applied with `brew bundle`.
#  ⚠️  NEVER add Wispr Flow to this file — it is installed manually,
#      last, outside brew, to capture a referral credit.
# ═══════════════════════════════════════════════════════════════

# ── CLI tools ──────────────────────────────────────────────────
brew "git"        # version control (newer than the CLT copy)
brew "gh"         # GitHub CLI — auth, repo create, clone
brew "node"       # needed for npx-launched MCP servers (PAI dependency)
brew "uv"         # fast Python package manager (PAI dependency)
brew "jq"         # JSON on the command line (used by scripts/trust-fleet.sh)

# ── GUI apps ───────────────────────────────────────────────────
cask "claude-code"           # Claude Code CLI (stable channel)
cask "claude"                # Claude desktop app (bundles its own Node for MCP extensions)
cask "raycast"               # launcher / productivity
cask "visual-studio-code"    # editor
